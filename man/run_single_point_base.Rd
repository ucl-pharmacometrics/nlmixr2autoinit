% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_single_point_base.R
\name{run_single_point_base}
\alias{run_single_point_base}
\title{Run Adaptive Single-Point PK Analysis}
\usage{
run_single_point_base(
  dat,
  route = c("bolus", "oral", "infusion"),
  half_life = NULL,
  dose_type = NULL,
  pooled_ctrl = pooled_control(),
  ssctrl = ss_control()
)
}
\arguments{
\item{dat}{A data frame with pharmacokinetic data. Must include key variables such as
\code{ID}, \code{DV}, \code{TIME}, \code{tad}, \code{recent_ii}, \code{dose}, \code{routeobs}, \code{iiobs}, \code{EVID}, etc.}

\item{route}{Character string. Must be one of "bolus", "oral", or "infusion".}

\item{half_life}{Optional numeric value. If NULL, it will be estimated from the data.}

\item{dose_type}{Optional character string (e.g. "bolus", "infusion"). Required if \code{half_life} is NULL.}

\item{pooled_ctrl}{Optional list of control settings from \code{\link{pooled_control}()}.}

\item{ssctrl}{Optional list of control settings from \code{\link{ss_control}()}.}
}
\value{
A named list with:
\describe{
\item{summary}{A data frame with trimmed mean CL and Vd, and timestamp}
\item{dat}{The updated full dataset}
\item{cl_df}{Subset used for clearance calculation}
\item{vd_df}{Subset used for volume distribution estimation}
}
}
\description{
Computes both clearance (CL) and volume of distribution (Vd) using adaptive single-point methods
from pharmacokinetic data. Automatically estimates half-life if not provided.
}
\examples{
\dontrun{
dat <- Bolus_1CPT
out <- processData(dat)
fdat<- out$dat
froute <-out$Datainfo$Value[out$Datainfo$Infometrics == "Dose Route"]
half_life <- get_hf(dat = fdat)$half_life_median
run_single_point_base(dat = fdat, half_life = half_life,route=froute)
}

}
